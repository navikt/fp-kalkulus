Lokalt databaseoppsett for windows uten docker
1) Legg til postgres (C:\Program Files\PostgreSQL\12\bin) i pathvariabelen i miljøvariabler
2) I eit kommandovindu med administratortilganger kjør følgende kommandoer:
	initdb -D \kalkulus\data
	pg_ctl -D \kalkulus\data -o "-p 5432" -l \kalkulus\logs start
	createuser --port=5432 -s postgres
3) Åpne appen SQL SHELL (pqsl) ved å søke etter den i windows søkefeltet. Gjør følgende valg
	Server [localhost]: (trykk enter)
	Database [postgres]: (trykk enter)
	Port [5432]: 5432
	Username [postgres]: (trykk enter)
Du får sannsynligvis beskjeden
WARNING: Console code page (850) differs from Windows code page (1252)
         8-bit characters might not work correctly. See psql reference
         page "Notes for Windows users" for details.
Type "help" for help.

4) Når logget inn som bruker postgres i psql, kjør kommandoene i skriptet fpkalkulus.sql:
CREATE DATABASE fpkalkulus_unit;
CREATE USER fpkalkulus_unit WITH PASSWORD 'fpkalkulus_unit';
GRANT ALL PRIVILEGES ON DATABASE fpkalkulus_unit TO fpkalkulus_unit;

CREATE DATABASE fpkalkulus;
CREATE USER fpkalkulus WITH PASSWORD 'fpkalkulus';
GRANT ALL PRIVILEGES ON DATABASE fpkalkulus TO fpkalkulus;
